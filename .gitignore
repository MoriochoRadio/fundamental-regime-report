# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.installed.cfg
*.egg

# --- Virtual environments ---
.venv/
venv/
env/
ENV/

# --- Environment / secrets ---
.env
.env.*
!.env.example
*.key
*.pem
secrets/
credentials/

# --- Jupyter / IPython ---
.ipynb_checkpoints/
*.ipynb_checkpoints

# --- Data & model artifacts ---
# (디렉터리 구조 합의 후 구체화 예정 — 지금은 광범위하게 차단)
data/raw/
data/interim/*
data/processed/
data/cache/
# 예외: LLM 빌드타임 배치 서술 산출물은 *추적*한다 (재현성·데모 안정성 —
#       비결정 LLM 출력을 1회 생성 후 고정. 대시보드는 이 정적 YAML 만 읽음).
#       (data/interim/* 로 내용물만 제외 → 하위 llm_interpretations/ 재포함 가능)
!data/interim/llm_interpretations/
# NOTE: data/external/ 는 *추적*한다. 외부 시스템에서 수동 다운로드한
#       정적 데이터(KOSPI200 분기 CSV 등) + 매니페스트가 들어가며,
#       재현성을 위해 git에 보관된다. CLAUDE.md §8.3 참조.
models/checkpoints/
models/artifacts/
*.parquet
*.feather
*.pkl
*.joblib
*.h5
*.onnx

# --- Logs / runs ---
logs/
runs/
mlruns/
wandb/
.lightning_logs/
tensorboard/

# --- Coverage / testing ---
.coverage
.coverage.*
htmlcov/
.tox/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# --- IDE / editor ---
.vscode/
.idea/
*.swp
*.swo
*~

# --- OS ---
.DS_Store
Thumbs.db
desktop.ini

# --- Claude Code (로컬 설정) ---
.claude/

# --- 프로젝트 산출물 임시 ---
*.tmp
*.bak
*.old
