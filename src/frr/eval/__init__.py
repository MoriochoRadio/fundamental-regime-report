"""Evaluation primitives — walk-forward splits, metrics (D8, 단계 2).

본 패키지는 단계 2 의 *시간 차원* 정직성 (PROGRESS §5.5.12). 향후 D8 metrics·
calibration·bootstrap 모듈이 같은 그룹으로 자연 확장 — *점진 생성* (CLAUDE.md §8.6).
"""
